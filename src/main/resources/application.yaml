server:
  port: 9001
  servlet:
    context-path: "/api/v0.1/"
spring:
  application:
    name: "PollutionMonitor"
  main:
    banner-mode: "console"
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: "validate"
  datasource:
    platform: "postgres"
    url: "jdbc:postgresql://localhost:7002/pollution_monitor"
    username: "admin"
    password: "secret"

esa-integration:
  url: "https://public-esa.ose.gov.pl"