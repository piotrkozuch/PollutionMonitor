server:
  port: 9001
  servlet:
    context-path: "/api/v0.1/"
spring:
  application:
    name: "PollutionMonitor"
  main:
    banner-mode: "console"
  jpa:
    hibernate:
      ddl-auto: "validate"
  datasource:
    platform: "postgres"
    url: "jdbc:postgresql://database:5432/pollution_tracker"
    username: "admin"
    password: "secret"

esa-integration:
  ulr: "https://public-esa.ose.gov.pl"